<%- include('layouts/header', { title: 'Dashboard' }) %>

<div class="container mt-5">
    <h1>Fitness Tracker</h1>
    <h2>Dashboard</h2>
    <p>Welcome, <%= user.username %>!</p>

    <div class="row">
        <!-- Left Column - Profile Info -->
        <div class="col-md-6">
            <div class="card mb-4 h-100"> <!-- Added h-100 for equal height -->
                <div class="card-header bg-primary text-white">
                    <h3>Your Profile</h3>
                </div>
                <div class="card-body d-flex flex-column"> <!-- Flex column layout -->
                    <ul class="list-unstyled flex-grow-1"> <!-- Takes available space -->
                        <li class="mb-2"><strong>Age:</strong> <%= profile.age || 'Not set' %></li>
                        <li class="mb-2"><strong>Height:</strong> <%= profile.height ? profile.height + ' cm' : 'Not set' %></li>
                        <li class="mb-2"><strong>Weight:</strong> <%= profile.weight ? profile.weight + ' kg' : 'Not set' %></li>
                        <li class="mb-3"><strong>Goal:</strong> <%= profile.goal ? profile.goal.replace('_', ' ') : 'Not set' %></li>
                    </ul>
                    <div class="mt-auto"> <!-- Pushes button to bottom -->
                        <a href="/update-profile" class="btn btn-primary">Update Profile</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Right Column - Workout Plan -->
        <div class="col-md-6">
            <div class="card h-100"> <!-- Added h-100 for equal height -->
                <% if (trainingPlan) { %>
                <div class="card-header bg-success text-white">
                    <h3>Your Recommended Plan</h3>
                </div>
                <div class="card-body d-flex flex-column"> <!-- Flex column layout -->
                    <div class="flex-grow-1"> <!-- Takes available space -->
                        <h5 class="card-title"><%= trainingPlan.title %></h5>
                        <p class="card-text"><%= trainingPlan.description %></p>
                    </div>
                    <div class="mt-auto"> <!-- Pushes button to bottom -->
                        <a href="/training-plan" class="btn btn-success">View Full Plan</a>
                    </div>
                </div>
                <% } else { %>
                <div class="card-body text-center d-flex flex-column"> <!-- Flex column layout -->
                    <div class="flex-grow-1"> <!-- Takes available space -->
                        <p>We're preparing your personalized workout plan.</p>
                    </div>
                    <div class="mt-auto"> <!-- Pushes button to bottom -->
                        <a href="/training-plan" class="btn btn-success">Get Your Workout Plan</a>
                    </div>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</div>

<%- include('layouts/footer') %>